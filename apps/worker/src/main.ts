// Background worker — processes indexing and watch jobs enqueued by the API.
// Job handlers will be registered here in M1 once the queue adapter is wired.

console.log("[atlas-worker] ready — waiting for jobs");

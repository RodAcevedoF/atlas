{
  "name": "atlas",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "prepare": "husky",
    "dev": "bun run redis:up && bun nx run-many -t dev --parallel=4",
    "redis:up": "docker compose up -d redis",
    "redis:down": "docker compose down",
    "typecheck": "bun nx run-many -t typecheck",
    "test": "bun nx run-many -t test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "build": "bun nx run-many -t build",
    "dataset:generate": "node --experimental-transform-types apps/api/scripts/datasets.ts generate data/course",
    "dataset:seed": "node --env-file=apps/api/.env --experimental-transform-types apps/api/scripts/datasets.ts seed"
  },
  "lint-staged": {
    "*.{js,jsx,mjs,cjs,ts,tsx,json,jsonc,css}": "biome check --write --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@tailwindcss/postcss": "^4.3.1",
    "bun-types": "^1.3.13",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "nx": "23.1.1",
    "tailwindcss": "^4.3.1",
    "typescript": "^5.7.3"
  }
}

{
  "name": "@atlas/domain",
  "projectType": "library",
  "tags": ["scope:domain", "type:lib"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "{projectRoot}"
      }
    }
  }
}
